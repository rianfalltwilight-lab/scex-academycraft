![title]
能源桥

![brief]
能源桥用于在虚能（IF）与 NeoForge 能量（FE/RF）之间进行转换。

![content]
能源桥分为输入和输出两种。输入能源桥接收有线 RF/FE，并通过 AcademyCraft 无线节点向网络提供 IF；输出能源桥从无线节点接收 IF，再向相邻的 RF/FE 机器或线缆供能。

两种能源桥都要先在无线页面连接 AcademyCraft 节点。有线能量设备可以连接能源桥任意一面。

换算关系为：**1 IF = 4 RF/FE**。能源桥可缓存 2000 IF，无线侧带宽为 100 IF/t。
