# Media Player 1.0.7 parity: pause/resume only AcademyCraft's own stream.
# SoundManager.pause()/resume() affect every sound, so expose the mapped
# instance channel instead of muting or pausing the entire game.
public net.minecraft.client.sounds.SoundManager soundEngine
public net.minecraft.client.sounds.SoundEngine instanceToChannel
