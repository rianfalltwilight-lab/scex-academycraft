# 0.0.28-scex-machineui

Minecraft 1.21.1 / NeoForge 21.1.248 非官方开发预览。网络协议升至 **v18 / data-v4**：客户端与服务端必须一起更新，不能与 v17 混用。存档格式保持不变。

- 修复同坐标替换节点后，客户端旧实例较高的修订号覆盖新实例名称的问题。菜单按加载实例身份接收名称更新，切换实例时正确重置修订；原有名称、密码、所有者和保存格式不变。
- 按固定上游界面布局恢复 EXP 细条、背景、位置和文字锚点。
- 修复满 EXP 时按钮重复绘制 LEARN 文字，恢复透明混合和可升级时的标签布局。
- 保留 0.0.27 及此前机器界面、无线网络与实体投射物修复；上游署名与资源声明不变，详见 NOTICE。

冻结交付验证：343 项 JUnit，0 失败/错误/跳过；Vicerach 实际客户端 14/14 命名、重开、连接和实例替换场景通过；最终按钮调整后另通过 6/6 界面场景（GUI 2/3/4，EXP 25%/50%/100%）。两次成功客户端均正常保存退出，235 份证据已逐项核验。

14 场景与最终 6 场景之间只有技能树 GUI 类改变，节点及网络类字节一致。最终冻结 JAR 与最后受测候选仅构建元数据不同；单个匿名类的 javac 局部变量调试名称变化不改变指令。此前失败的测试与夹具修复保留在研发记录，不宣称零警告或全能力复刻完成。

完整玩家整包、多人、独立专用服务器、性能、自定义资源包为 NOT_RUN。本次只同步 GitHub，不部署生产。

公开附件保持冻结 JAR 的代码和游戏资源，仅补充公开许可与构建记录；原始和公开 SHA-256、打包差异见 release-manifest.json。源码 ZIP 来自公开仓库，不包含世界、运行日志或依赖副本。
